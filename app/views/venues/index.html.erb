<h1>Venues Index</h1>

<% @venues.each do |venue| %>
<%= link_to venue.name, venue_path(venue) %> <br>
<%= venue.address %> <br>
<% end %>

<br><br>
<%= link_to "List a new venue", new_venue_path %>
<div id="map"></div>

<script>
  var map;
  function initMap() {
    map = new;
    debugger;

     google.maps.Map(document.getElementById('map'), {
      center: {lat: 40.749372, lng: -73.985584},
      zoom: 12
    });
  }
//   function myFunc() {
//     var lat = '';
// var lng = '';
// // var address = {zipcode} or {city and state};
// var zipcode = 10001;
// geocoder.geocode( { 'zipcode': zipcode}, function(results, status) {
//
//   if (status == google.maps.GeocoderStatus.OK) {
//      lat = results[0].geometry.location.lat();
//      lng = results[0].geometry.location.lng();
//     });
//   } else {
//     alert("Geocode was not successful for the following reason: " + status);
//   }
// });
// alert('Latitude: ' + lat + ' Logitude: ' + lng);
//   };
//   myFunc();
</script>
<script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyB4xLCjwQBDmJnqibgO8TqFuLv8Qj2kobM&callback=initMap"
async defer></script>
